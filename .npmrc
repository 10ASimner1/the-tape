# Lifecycle scripts are the classic supply-chain malware vector — the exact thing
# this project records. Committed here so it applies to every invocation, including
# a nested one that forgets the flag.
ignore-scripts=true

# Exact versions only. A caret range means the lockfile is the only thing standing
# between you and a version nobody reviewed.
save-exact=true
